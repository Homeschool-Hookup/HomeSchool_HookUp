<div class="field">
    <label class="label">Email</label>
    <div class="control has-icons-left has-icons-right">
        <input class="input is-link" type="email" placeholder="Email input" value="{{user.email}}">
        <span class="icon is-small is-left">
            <i class="fas fa-envelope"></i>
        </span>
    </div>

    <label class="label">Password</label>
    <div class="control has-icons-left has-icons-right">
        <input class="input is-link" type="email" placeholder="Password input" value="{{user.password}}">
        <span class="icon is-small is-left">
            <i class=""></i>
        </span>
    </div>
</div>

<div class="buttons">
    <button class="button is-warning" id="signUpBtn">Link</button>
</div>