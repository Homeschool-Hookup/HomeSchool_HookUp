<div class="columns">
    <div class="column">
        <h1 class="has-text-danger is-size-2">Update Your Post</h1>
        <div class="field">
            <label class="label">Title</label>
            <div class="control has-icons-left has-icons-right">
                <input class="input is-link" type="text" placeholder="Title" value="{{post.title}}" id="selfTitle">
            </div>
        </div>
        <div class="field">
            <label class="label">Please include a description of what helped you and/or your family unwind, as well
                as
                any resources you've found helpful!</label>
            <input class="input is-link" type="text" placeholder="What helps you unwind?" value="{{post.content}}"
                id="selfContent">
        </div>
        <div class="field">
            <div class="control">
                <button class="button is-warning" id="submitCare">Update</button>
            </div>
        </div>
    </div>
</div>